<?xml version="1.0" encoding="UTF-8"?>
<playlist version="1" xmlns="http://xspf.org/ns/0/">
    <title>{{ audiosource.title }}</title>
    <creator></creator>
    <link></link>
    <info>{% if audiosource.description %}{{ description }}{% endif %}</info>
    <image></image>

    <trackList>

      {% for audiofile in audiosource.audio_files.all %}
        <track>
          <location>{{ audiofile.file.url }}</location>
          <creator>{{ audiofile.artist }}</creator>
          <album></album>
          <title>{{ audiofile.title }}</title>
          <annotation></annotation>
          <duration>{{ audiofile.length }}</duration>
          <image></image>
          <info></info>
          <link></link>
        </track>
      {% endfor %}
    </trackList>
</playlist>
